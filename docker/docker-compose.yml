# ====================================================
# Project QuantumLedger - Production Web Stack
# ====================================================

version: '3.8'

services:
  # CBDC Operations Dashboard Web App
  dashboard:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    container_name: quantumledger-dashboard
    ports:
      - "80:80"
    restart: unless-stopped
